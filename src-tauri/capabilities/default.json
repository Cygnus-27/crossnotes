{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities. Deliberately minimal: the webview gets a file picker and a reveal-in-folder action, nothing more. All filesystem and network work happens in Rust, where paths can be validated before use, so the fs and shell plugins are not granted at all.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir"
  ]
}
